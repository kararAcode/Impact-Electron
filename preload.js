
const {spriteDrawing} = require('./scripts/spriteDrawing')
// window.$ = window.jQuery = require('jquery');

window.onload = () => {
      window.$ = window.jQuery = require('jquery');

      spriteDrawing();
}